{
  "name": "respireLYF-blog-pipeline",
  "version": "1.0.0",
  "description": "Multi-agent blog pipeline for RespireLYF — trend scraping to Payload CMS publishing",
  "private": true,
  "scripts": {
    "build": "cross-env NODE_OPTIONS=--max-old-space-size=4096 tsc",
    "tier1": "npm run build && node dist/pipeline/tier1-orchestrator.js",
    "tier2": "npm run build && node dist/pipeline/tier2-orchestrator.js",
    "post-blog": "node post-blog.js",
    "type-check": "cross-env NODE_OPTIONS=--max-old-space-size=4096 tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.39.0",
    "axios": "^1.7.2",
    "dotenv": "^16.4.5",
    "rss-parser": "^3.13.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "cross-env": "^7.0.3",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.4.5"
  }
}
